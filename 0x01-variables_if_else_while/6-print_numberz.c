#include <stdio.h>

/**
 * main - prints single digits in base 10
 * Return: Always 0
 */

int main(void)
{
	int c;

	while
		(c < 10)
		{
		putchar(c + '0');
		c++;
		}

	putchar('\n');

	return (0);
	}
